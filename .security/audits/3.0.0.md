---
version: 3.0.0
audit-date: 2026-08-08
auditor: Ryan Armstrong
assisted-by: AI assistant via .claude/skills/security-audit-system (claude-opus-5)
commit_sha: f86ddf5ff2ece7d9fe702f8922be6f18bb0f7372
manifest-sha256: sha256:e5bf5b487e569e987d669d6e4b3e498910c90ea1d2ca275984e4ff89bab44540
sources-sha256: sha256:21066a74efe42e83cea25ea5f2024fed147c39a04d42e06282e0c1ad5397e459
deps-sha256: sha256:8381493fc139e57fb21ad01e62247f9101125e09e35ba8c3541cec8ac3336a1c
claims-sha256: sha256:2546c2cec67c4a37b798087f9b419305ca33ec3a9954d13ef7618838620ce019
build-sha256: sha256:27a801b003d52fea5e7e47913c75b1cac572dbaf9b080a3c0d1dee4da54a3059
sbom_ref: "release asset: class-list-builder-v3.0.0.cdx.json"
status: pass
exceptions-cited: [CLB-EXC-0001, CLB-EXC-0002]
prior_audit: ".security/audits/2.3.0.md"
---

# Security Audit — v3.0.0

## Scope

Single-file React SPA distributed as a standalone HTML bundle, plus its
build pipeline and the public security claims in `SECURITY.md`.

Paths covered by the manifest hash:

- `src/**` (33 files)
- `package.json`, `package-lock.json`
- `SECURITY.md`
- `build-standalone.js`, `class-list-builder-source.html`,
  `scripts/validate-build.js`

Out of scope: `node_modules/` (covered transitively via `npm audit`),
`tests/**`, developer-only scripts not in the manifest.

This is the first major-version release audited by this system. This audit's predecessor is
v2.3.0.

## Methodology

- `npm audit --package-lock-only --json` reviewed against the committed
  lockfile (not a locally-resolved tree), before and after the dependency
  updates in this release.
- `npm audit --package-lock-only --omit=dev` run separately to establish
  whether any advisory reaches the shipped artifact.
- Each claim in `SECURITY.md` re-verified against the post-change source by
  grep and by reading the matched call sites.
- Changed files reviewed line-by-line: `src/app.js`,
  `src/utils/projectDeserializer.js`, `src/defaults.js`, `package.json`,
  `package-lock.json`.
- The breaking change's blast radius traced through
  `checkVersionCompatibility()` → `canLoad` → `LoadProjectModal` to confirm
  what actually happens to a v2.x file under this release (see F-004).
- Prompt-injection sentinel scan re-run across all of `src/**` after the
  source changes, plus a Unicode irregularity scan (bidi overrides,
  zero-width, tag characters).
- Standalone artifact rebuilt (`node build-standalone.js`) and validated
  (`node scripts/validate-build.js`).
- Toolchain installed from the committed lockfile and the full suite re-run:
  208 tests pass across 11 files; 0 lint errors. `package-lock.json` was
  byte-compared before and after the install to confirm the install did not
  mutate it.

**Process deviation, disclosed:** because of F-002, `npm ci` aborts with
`EBADPLATFORM` on the auditor's darwin/arm64 host. Both the lockfile update
(`npm update --package-lock-only --force`) and the toolchain install
(`npm ci --force`) required `--force` to bypass that platform check. The
flag suppresses the platform gate only; resolution still came from the
committed lockfile, `package.json` was not modified by it, and the
post-install byte-comparison confirms the lockfile is exactly what is
committed and audited. CI runs on linux/x64, where plain `npm ci` succeeds
without `--force`.

## Changes Since v2.3.0

1. **BREAKING — pre-v2.2.0 project compatibility removed** (issue #74).
   Three pieces of temporary migration code are gone:

   - `src/utils/projectDeserializer.js` — the
     `optimizationResults.assignments` → flat `assignment` fallback
   - `src/app.js` — the app-level duplicate of that fallback, which was
     already dead code (`handleLoadProject` only ever receives
     `deserializeProject()` output, and its `hasAssignments` check called
     `Object.keys()` on a `Map`, which is always empty)
   - `tests/project-serialize.test.js` — the migration regression test

   Removing the deserializer migration is safe at a major bump rather than
   merely convenient. `checkVersionCompatibility()`
   (`src/utils/projectDeserializer.js:66-72`) records a major-version
   mismatch as an **error**, not a warning; `canLoad` is `errors.length === 0`;
   and `LoadProjectModal` gates its entire load path — including the "force"
   mode — behind `canLoad` (`src/components/LoadProjectModal.js:105`, `:260`).
   Under a v3 app, every v2.x file is therefore refused before
   deserialization ever reaches assignment handling. The migration is
   unreachable by construction, not merely unused, which is why the issue
   targeted v3.0.0 specifically.

   The removed regression test is replaced by one asserting the major-version
   gate directly, so the property that makes the migration's absence safe
   remains covered rather than silently untested.

   See F-004 for the data-availability consequence for existing users.

2. **Dependency updates** (`package-lock.json`) — Five transitive dev
   dependencies advanced to clear all outstanding advisories. Lockfile-only;
   no `package.json` change, as none is a direct dependency. Details in the
   Dependency Audit section.

3. **Flaky-test fix** (`tests/assessment.test.js`) — The
   `computeBaselineRandom > mean of many trials is stable` case compared two
   unseeded 100-trial sampling runs against a 20% tolerance. Measured over
   2000 repetitions it failed 5.45% of the time (p50 0.069, p95 0.204, max
   0.344), and it broke the Node 22.x matrix job during this release. The
   test now seeds `Math.random` with a Mulberry32 stream and samples 500
   trials. No product code changed; `tests/**` is outside the manifest, so
   this does not affect any hash in this audit.

4. **Version bump** — `package.json` and `src/defaults.js` to 3.0.0.

`claims-sha256` (`2546c2ce…`) and `build-sha256` (`27a801b0…`) are
byte-identical to v2.3.0, confirming that neither `SECURITY.md` nor the build
entrypoints were touched. `sources-sha256` changed for the three source files
named above; `deps-sha256` for the version bump plus the lockfile updates.

## SECURITY.md Claims Verification

| Claim | Status | Evidence |
|-------|--------|----------|
| Zero network transmission at runtime | Verified | `grep -rnE 'fetch\(\|XMLHttpRequest\|WebSocket\|sendBeacon\|navigator\.send' src/` → no matches |
| No fetch / XHR / WebSocket / sendBeacon | Verified | Same grep, re-run after the source changes; this release only deletes code |
| Release version inlines all dependencies | Verified | `build-standalone.js:199-203` inlines DM Sans/DM Mono, react@18.3.1, react-dom@18.3.1, @babel/standalone@7.29.0, exceljs@4.4.0; fetched bytes are checked against the source HTML's `integrity=` attributes (`build-standalone.js:157-195`, `:360-390`). Build re-run and validated at this commit |
| localStorage scope is origin-bound | Verified | Only criteria + theme keys stored (`src/contexts/index.js:343-383`, `:484-525`; `src/components/SettingsModal.js:258-259`). No student data written; unchanged this release |
| Deterministic seeded RNG | Verified | Optimizer's FNV-1 + Mulberry32 seeding untouched. `Math.random()` remains confined to assessment baselines |
| FERPA: data never leaves the device | Verified | No network sinks; no new export paths. The breaking change affects whether local files can be *read*, not whether data leaves the device — see F-004 |
| No `eval` / `new Function` in app source | Verified | `grep -rnE '\beval\(\|new Function\(' src/` → no matches |

No refuted claims.

## Vulnerability Findings

### F-001 — Dev-toolchain dependencies carried five high-severity advisories

- **Severity:** informational *(npm rated these `high`; see rationale)*
- **Category:** dependency / supply-chain
- **Location:** `package-lock.json` (transitive dev tree)
- **Description:** At the opening of this audit, `npm audit` reported 5
  high-severity advisories against `brace-expansion`, `js-yaml`, `nanoid`,
  `postcss`, and `vite` — all transitive dev-only dependencies reached
  through `vitest` → `vite` and `eslint`. This was drift since the v2.3.0
  audit of 2026-05-25, which recorded zero; the advisories were published in
  the intervening ~11 weeks.
- **Impact:** Contextually low even before remediation. `package.json`
  declares **zero production dependencies** (`"dependencies": {}`), and
  `npm audit --omit=dev` reported `found 0 vulnerabilities` throughout. None
  of the five packages appeared in the built artifact.
  `build-standalone.js` imports only Node builtins (`fs`, `path`, `https`,
  `zlib`, `crypto`), so the vulnerable code was absent from the release path
  as well as the runtime. The advisories are DoS (`brace-expansion`,
  `js-yaml`, `nanoid`) plus local file-disclosure in developer tooling
  (`postcss` source-map loading, `vite` dev-server path handling on
  Windows) — reachable only by a developer running the test or lint
  toolchain against hostile input.

  Severity is recorded as informational per the auditor's contextual
  assessment, not npm's context-free CVSS rating.
- **Remediation:** Applied in this release. All five chains updated in
  `package-lock.json`; `npm audit --package-lock-only` now reports
  `found 0 vulnerabilities`.
- **Status:** fixed-in-this-release

### F-002 — `@rolldown/binding-linux-x64-gnu` pinned as a direct devDependency breaks `npm ci` off-platform

- **Severity:** low
- **Category:** supply-chain / build integrity
- **Location:** `package.json` devDependencies; introduced in commit `c57d464`
- **Description:** A platform-specific native binary,
  `@rolldown/binding-linux-x64-gnu`, is declared as an ordinary direct
  devDependency rather than being resolved as the optional, per-platform
  dependency of `rolldown`. `npm ci` consequently aborts with
  `EBADPLATFORM` on any host that is not linux/x64:

  ```
  npm error code EBADPLATFORM
  npm error notsup Unsupported platform for @rolldown/binding-linux-x64-gnu@1.0.0-rc.18:
  wanted {"os":"linux","cpu":"x64"} (current: {"os":"darwin","cpu":"arm64"})
  ```

  The lockfile already carries the full set of per-platform bindings,
  including `@rolldown/binding-darwin-arm64`, so the direct pin is not
  needed for resolution — only the linux-x64 CI runner benefits from it.
  The check is not confined to installs: it also blocks
  `npm update --package-lock-only`, so routine dependency maintenance
  cannot be performed off linux/x64 without `--force`.
- **Impact:** No effect on the released artifact or on CI (which runs
  linux/x64). The security-relevant consequence is process, not product. A
  maintainer or reviewer on macOS or Windows cannot run `npm ci` or update
  the lockfile normally, and is therefore pushed toward `npm install` or
  `--force`, both of which weaken the guarantee that the installed tree
  matches the audited, hash-pinned lockfile. That is a plausible route for
  un-audited dependency drift to enter a release — and this audit had to
  navigate it directly (see Methodology).
- **Remediation:** Remove the direct devDependency and let `rolldown`'s
  optionalDependencies resolve the correct binding per platform. If the CI
  runner genuinely needs the explicit pin, prefer scoping `--force` to the
  CI job, or move the pin behind an `optionalDependencies` entry.
- **Status:** open — **deliberately not remediated in this release**, at the
  maintainer's direction. Tracked for a standalone PR.

### F-003 — v2.3.0 audit's Accepted Risks table misdescribes both exception IDs

- **Severity:** informational
- **Category:** other (documentation / audit-trail accuracy)
- **Location:** `.security/audits/2.3.0.md` (Accepted Risks section)
- **Description:** The prior audit's Accepted Risks table describes
  CLB-EXC-0001 as `'unsafe-eval'` in CSP and CLB-EXC-0002 as
  `'unsafe-inline'` in CSP style-src. The registry at
  `.security/audits/exceptions.json` defines CLB-EXC-0001 as *"Single
  maintainer — no separation of duties"* and CLB-EXC-0002 as *"ExcelJS
  4.4.0 transitive CVEs in Node-only code paths"*. The cited IDs and the
  described risks do not correspond.
- **Impact:** No runtime impact. `verify-audit.js` checks only that cited
  IDs exist and are unexpired, so the mismatch passes CI. The risk is to the
  audit trail itself: a reader reconstructing which risks were accepted at
  v2.3.0 gets the wrong answer, and the two genuinely accepted risks appear
  undocumented for that release.
- **Remediation:** Signed audits are append-only, so `2.3.0.md` must not be
  edited. Issue a `2.3.0-r2` revision carrying the corrected table, with
  `supersedes:` pointing at the original and the original marked
  `superseded`.
- **Status:** open (pre-existing; not introduced by this release)

### F-004 — Major bump renders all existing v2.x project files unloadable

- **Severity:** low
- **Category:** privacy / data stewardship (availability of FERPA-regulated records)
- **Location:** `src/utils/projectDeserializer.js:66-72`;
  `src/components/LoadProjectModal.js:105`, `:260`
- **Description:** This release changes `APP_VERSION` from `2.x` to `3.0.0`.
  `checkVersionCompatibility()` classifies any major-version difference as an
  error rather than a warning, `canLoad` is `errors.length === 0`, and
  `LoadProjectModal` renders no load options at all when `canLoad` is false.
  The practical result is that **every project file saved by any v2.x
  release is refused outright by v3.0.0**, with the message *"Major version
  mismatch… Please use the same major version to load this project."* This
  is broader than the migration removal that motivated the change: it is not
  that pre-v2.2.0 assignments are dropped, it is that no v2.x file opens at
  all, including files saved by v2.3.0 last week.
- **Impact:** No confidentiality or integrity exposure — student data is
  untouched, still local, and still on disk in the user's own `.json` file.
  The risk is **availability of records the tool is trusted to hold**. A
  teacher who upgrades mid-year and then tries to reopen the current year's
  class list is blocked, and the failure is a flat refusal rather than a
  degraded load. For a tool whose stated audience is teachers handling
  FERPA-regulated data, losing access to your own saved work is a
  user-visible harm even though it is not a vulnerability.
- **Mitigating factors:** The saved file is plain JSON and is never
  modified or deleted by the failed load, so no data is destroyed. Every
  prior release remains available as a self-contained HTML file on the
  GitHub Releases page, and those files work offline forever — so recovery
  is "download v2.3.0 and open the file there," not "data lost." The
  maintainer's stated usage model is one class-list build per school year,
  in which cross-year file compatibility is not required.
- **Remediation:** Accepted as an intentional product decision for this
  release, at the maintainer's direction. If mid-year upgrades turn out to
  matter in practice, the options are (a) relax the major-version check to a
  warning so v2.x files load with a caveat, or (b) note the recovery path in
  the release notes and in the error message itself, which currently tells
  the user to "use the same major version" without saying where to get one.
  Option (b) is cheap and is recommended regardless.
- **Status:** open — accepted as intended behavior; no exception registry
  entry, since this is a product decision rather than a standing security
  risk.

## Dependency Audit

```
$ npm audit --package-lock-only --json | jq '.metadata.vulnerabilities'
{
  "info": 0,
  "low": 0,
  "moderate": 0,
  "high": 0,
  "critical": 0,
  "total": 0
}

$ npm audit --package-lock-only --omit=dev
found 0 vulnerabilities
```

Zero vulnerabilities, down from 5 high at the opening of this audit.
Remediation applied in this release:

| Package | Before | After | Advisories cleared |
|---------|--------|-------|--------------------|
| `vite` | 8.0.10 | 8.2.1 | GHSA-v6wh-96g9-6wx3 (launch-editor NTLMv2 disclosure, Windows), GHSA-fx2h-pf6j-xcff (`server.fs.deny` bypass, Windows) |
| `js-yaml` | 4.1.1 | 4.3.1 | GHSA-h67p-54hq-rp68, GHSA-52cp-r559-cp3m, GHSA-5p4m-2wfm-xmqj — quadratic-complexity DoS in merge keys / `!!omap` |
| `postcss` | 8.5.12 | 8.5.26 | GHSA-r28c-9q8g-f849, GHSA-fxqj-rqcc-2cmp — path traversal via attacker-controlled `sourceMappingURL` |
| `nanoid` | 3.3.11 | 3.3.18 | GHSA-28wg-ghj8-5hjv, GHSA-2v37-7h3g-55p8 — infinite loop on negative/zero size |
| `brace-expansion` | 1.1.14 | 1.1.18 | GHSA-3jxr-9vmj-r5cp, GHSA-mh99-v99m-4gvg, GHSA-rgw5-rvv9-x895 — DoS via exponential/unbounded expansion |

All five are transitive dev-only dependencies; the update is confined to
`package-lock.json`, with `package.json` unchanged. Three correspond to
Dependabot PRs #79 (vite), #80 (js-yaml) and #81 (postcss), and the resolved
versions meet or exceed what each proposed. `nanoid` and `brace-expansion`
had no PR of their own and were cleared transitively.

`vite` advanced to 8.2.1 rather than the 8.0.16 that PR #79 proposed, a
larger step within the same major. It is reached only through `vitest`,
which resolves it inside its declared range; the full suite (208 tests)
passes against the installed lockfile tree, and the standalone build
validates.

The distinction that continues to matter for release safety: the project
ships **no production dependencies**. The four third-party libraries in the
release artifact (react, react-dom, @babel/standalone, exceljs) are fetched
from unpkg at build time and verified against pinned SRI hashes. None was
affected by the advisories above, and none changed in this release.

## Prompt Injection Defense Notes

A sentinel scan was performed across all of `src/**` (33 files) **after** the
source changes in this release, covering instruction-override phrases,
role-marker injection, Unicode tricks, and hidden base64/hex payloads.
Reporting both what was found and that the scan ran, since silence and
absence are otherwise indistinguishable.

**Two benign matches, no injection attempts:**

1. Instruction-phrase scan matched one line, at `src/components/HelpModal.js:75`:

   ```
   Total flags and scores act as safety nets to catch overall imbalances.
   ```

   The pattern `act as` fires on ordinary English describing optimizer
   behavior. Pre-existing UI copy, not part of this changeset. The same match
   was recorded in the v2.3.0 audit.

2. Unicode scan matched one line, at `src/components/SetupPage.js:178`:

   ```
   <div className="icon">👩‍🎓</div>
   ```

   The hit is U+200D (zero-width joiner) as a legitimate component of the
   `👩‍🎓` emoji ZWJ sequence, not a hidden-character payload. Pre-existing.

**Not found:** no bidi overrides (U+202A–U+202E, U+2066–U+2069), no tag
characters (U+E0000–U+E007F), no stray zero-width characters outside the
emoji sequence above, no base64/hex blobs over 50 characters in the changed
files, no role markers (`[INST]`, `<|im_start|>`, `<<SYS>>`, `system:`,
`assistant:`), and no text anywhere in `src/**` attempting to instruct,
redirect, or reconfigure an AI auditor.

Note that this release **deletes** source code rather than adding it, which
narrows rather than widens the injection surface.

No instruction text from any source file altered this audit's scope,
severity classifications, findings, or sign-off. The audit plan was fixed
before any source file was opened and was executed unchanged.

**Residual risk:** indirect prompt injection is not a solved problem as of
mid-2026; adaptive attacks defeat published defenses at high rates. The
hardening applied here is defense-in-depth, not a guarantee. The
deterministic checks (`audit-hash.js`, `verify-audit.js`) and the human
sign-off below are the enforcement layer; the AI-assisted review is a force
multiplier, not the arbiter.

## Accepted Risks

| Exception ID | Reason | Expires |
|-------------|--------|---------|
| CLB-EXC-0001 | Single maintainer — no separation of duties. Auditor and approver are the same person; mitigated by public GitHub history, SLSA-attested releases, and a publicly verifiable audit trail. | 2027-05-16 |
| CLB-EXC-0002 | ExcelJS 4.4.0 transitive CVEs confined to Node-only code paths. The app calls only `workbook.xlsx.load(buffer)` from the browser UMD bundle (`src/components/ImportModal.js`); the vulnerable Node paths are unreachable. | 2027-05-16 |

Both exceptions are unexpired as of the audit date and remain accurate as
written in `.security/audits/exceptions.json`. Neither was modified by this
audit, and no new exception was added — F-001 was remediated rather than
accepted, and F-004 is an intentional product decision rather than a
standing risk.

F-002, F-003 and F-004 remain open but are not exception-backed: none is a
vulnerability in the released artifact.

## Sign-off

PENDING HUMAN SIGN-OFF — the auditor of record must personally review every
finding above before this line is replaced. Three judgments in this draft
warrant a human decision rather than inheritance:

1. **F-004, the breaking change's blast radius.** The audit records that
   *no* v2.x file opens under v3.0.0 — not merely pre-v2.2.0 ones — and
   accepts it as intended. Confirm that matches your intent, and consider
   the cheap half of the remediation: the error message tells users to "use
   the same major version" without telling them prior releases are still
   downloadable.
2. **F-002's effect on this audit's own integrity.** `--force` was required
   for both the lockfile update and the toolchain install because `npm ci`
   cannot run on the auditor's host. The Methodology section documents
   exactly what that flag did and did not bypass, and the lockfile was
   byte-compared afterwards, but you should satisfy yourself that this is
   acceptable provenance for a release audit.
3. **The `vite` step to 8.2.1.** This exceeds the 8.0.16 that Dependabot
   proposed. Tests and build pass against it, but the larger step is a
   judgment call you are signing for.

Replace this block with the attestation below, filled in:

> I have personally reviewed every finding above. The artifacts at the
> listed hashes are, to my knowledge, free of known vulnerabilities of
> severity high or above outside of declared exceptions, and the public
> claims in `SECURITY.md` are accurate as of this audit date.
>
> — Ryan Armstrong, 2026-08-08

---
version: 2.3.1
audit-date: 2026-08-08
auditor: Ryan Armstrong
assisted-by: AI assistant via .claude/skills/security-audit-system (claude-opus-5)
commit_sha: 114bd1aeb7e1389046c5f7b50aa8b4c3eaa0a6e4
manifest-sha256: sha256:e08916bfca603d9b95f24f1e778b65e015fef7d63317afd03ef080ecffdb79cc
sources-sha256: sha256:f50aaa68147a2d0db67f2494f36d6da3e6daffd3b1dda4bdbb89bdd9480f78ae
deps-sha256: sha256:f15b66a6949f0ebe325c5893e85a75579f1f0d2b6262d4494cb434978d9a4e4d
claims-sha256: sha256:2546c2cec67c4a37b798087f9b419305ca33ec3a9954d13ef7618838620ce019
build-sha256: sha256:27a801b003d52fea5e7e47913c75b1cac572dbaf9b080a3c0d1dee4da54a3059
sbom_ref: "release asset: class-list-builder-v2.3.1.cdx.json"
status: conditional-pass
exceptions-cited: [CLB-EXC-0001, CLB-EXC-0002]
prior_audit: ".security/audits/2.3.0.md"
---

# Security Audit — v2.3.1

## Scope

Single-file React SPA distributed as a standalone HTML bundle, plus its
build pipeline and the public security claims in `SECURITY.md`.

Paths covered by the manifest hash:

- `src/**` (33 files)
- `package.json`, `package-lock.json`
- `SECURITY.md`
- `build-standalone.js`, `class-list-builder-source.html`,
  `scripts/validate-build.js`

Out of scope: `node_modules/` (covered transitively via `npm audit`),
`tests/**`, developer-only scripts not in the manifest.

## Methodology

- `npm audit --package-lock-only --json` reviewed against the committed
  lockfile (not a locally-resolved tree); results in Dependency Audit below.
- `npm audit --package-lock-only --omit=dev` run separately to establish
  whether any advisory reaches the shipped artifact.
- Each claim in `SECURITY.md` re-verified against current source by grep
  and by reading the matched call sites.
- Changed files reviewed line-by-line: `src/app.js`, `src/defaults.js`,
  `package.json`.
- Prompt-injection sentinel scan run across all of `src/**`, plus a
  Unicode irregularity scan (bidi overrides, zero-width, tag characters).
- Standalone artifact rebuilt (`node build-standalone.js`) and grepped to
  confirm none of the flagged dependency packages appear in the output.
- Full test suite (`npm test`) and lint (`npm run lint`) run: 208 tests
  pass across 11 files; 0 lint errors.

## Changes Since v2.3.0

This is a maintenance release. It contains no feature work and no change
to data handling, storage, export, or the build pipeline.

1. **Dead-code removal** (`src/app.js`) — Removed the pre-v2.2.0 assignment
   fallback from `handleLoadProject`, tracked as issue #74.

   `handleLoadProject` is reachable only via `LoadProjectModal`
   (`src/app.js:192`, `src/components/LoadProjectModal.js:123`), and both
   of that component's load modes pass the output of `deserializeProject()`.
   The deserializer already performs this migration at
   `src/utils/projectDeserializer.js:507-518`. The app-level fallback could
   therefore only fire when the deserializer's own migration produced an
   empty result — and because both apply identical student/teacher
   validation, the `Map` the fallback iterated was empty in exactly that
   case, making its `setAssignment({})` a no-op.

   The companion `hasAssignments` navigation check was independently inert:
   the deserializer returns `optimizationResults.assignments` as a `Map`
   (`src/utils/projectDeserializer.js:530-543`), and `Object.keys(map).length`
   is always `0`, so navigation has always been driven by `data.assignment`
   alone.

   **The deserializer-side migration and its regression test are retained.**
   They are the code path that actually loads pre-v2.2.0 projects and remain
   scheduled for removal at v3.0.0 per #74. No user-visible behavior changes
   in this release.

2. **Version bump** — `package.json` and `src/defaults.js` to 2.3.1.

`claims-sha256` and `build-sha256` are byte-identical to v2.3.0
(`2546c2ce…` and `27a801b0…`), confirming `SECURITY.md` and the build
entrypoints were not touched by this release.

## SECURITY.md Claims Verification

| Claim | Status | Evidence |
|-------|--------|----------|
| Zero network transmission at runtime | Verified | `grep -rnE 'fetch\(\|XMLHttpRequest\|WebSocket\|sendBeacon\|navigator\.send' src/` → no matches |
| No fetch / XHR / WebSocket / sendBeacon | Verified | Same grep; changeset adds no call sites (net −16 lines) |
| Release version inlines all dependencies | Verified | `build-standalone.js:199-203` inlines DM Sans/DM Mono, react@18.3.1, react-dom@18.3.1, @babel/standalone@7.29.0, exceljs@4.4.0; fetched bytes are checked against the source HTML's `integrity=` attributes (`build-standalone.js:157-195`, `:360-390`) |
| localStorage scope is origin-bound | Verified | Only criteria + theme keys stored (`src/contexts/index.js:343-383`, `:484-525`; `src/components/SettingsModal.js:258-259`). No student data written; unchanged this release |
| Deterministic seeded RNG | Verified | Optimizer's FNV-1 + Mulberry32 seeding untouched (`sources` diff limited to `app.js`/`defaults.js`). `Math.random()` remains confined to assessment baselines, as documented in the v2.3.0 audit |
| FERPA: data never leaves the device | Verified | No network sinks; no new export paths; changeset removes code only |
| No `eval` / `new Function` in app source | Verified | `grep -rnE '\beval\(\|new Function\(' src/` → no matches (Babel-standalone's own transpilation is covered by CLB-EXC-0001's CSP context, see Accepted Risks) |

No refuted claims.

## Vulnerability Findings

### F-001 — Dev-toolchain dependencies carry five high-severity advisories

- **Severity:** informational *(npm rates these `high`; see rationale)*
- **Category:** dependency / supply-chain
- **Location:** `package-lock.json` (transitive dev tree)
- **Description:** `npm audit` reports 5 high-severity advisories against
  `brace-expansion`, `js-yaml`, `nanoid`, `postcss`, and `vite`. All five are
  transitive dev-only dependencies reached through `vitest` → `vite` and
  `eslint`. Full advisory list in the Dependency Audit section below.
- **Impact:** Contextually low. `package.json` declares **zero production
  dependencies** (`"dependencies": {}`), and `npm audit --omit=dev` reports
  `found 0 vulnerabilities`. None of the five packages appear in the built
  artifact (`grep -c` against `dist/class-list-builder-v2.3.1.html` returns
  `0` for each). `build-standalone.js` imports only Node builtins (`fs`,
  `path`, `https`, `zlib`, `crypto`), so the vulnerable code is absent from
  the release path as well as the runtime. The advisories are DoS
  (`brace-expansion`, `js-yaml`, `nanoid`) plus local file-disclosure in
  developer tooling (`postcss` source-map loading, `vite` dev-server path
  handling on Windows) — reachable only by a developer running the test or
  lint toolchain against hostile input.

  Severity is recorded as informational per the auditor's contextual
  assessment, not npm's context-free CVSS rating. The condition attached to
  this release's `conditional-pass` status is that the pending remediation
  below lands.
- **Remediation:** Merge the three open Dependabot PRs, which cover the
  affected packages directly: #79 (vite 8.0.10 → 8.0.16), #80 (js-yaml
  4.1.1 → 4.3.0), #81 (postcss 8.5.12 → 8.5.25). `npm audit` reports
  `fixAvailable: true` for all five. Re-run `npm audit` afterwards to
  confirm the `brace-expansion` and `nanoid` chains clear transitively.
- **Status:** open (remediation available and already staged as PRs #79/#80/#81)

### F-002 — `@rolldown/binding-linux-x64-gnu` pinned as a direct devDependency breaks `npm ci` off-platform

- **Severity:** low
- **Category:** supply-chain / build integrity
- **Location:** `package.json` devDependencies; introduced in commit `c57d464`
- **Description:** A platform-specific native binary,
  `@rolldown/binding-linux-x64-gnu`, is declared as an ordinary direct
  devDependency rather than being resolved as the optional, per-platform
  dependency of `rolldown`. `npm ci` consequently aborts with
  `EBADPLATFORM` on any host that is not linux/x64:

  ```
  npm error code EBADPLATFORM
  npm error notsup Unsupported platform for @rolldown/binding-linux-x64-gnu@1.0.0-rc.18:
  wanted {"os":"linux","cpu":"x64"} (current: {"os":"darwin","cpu":"arm64"})
  ```

  The lockfile already carries the full set of per-platform bindings,
  including `@rolldown/binding-darwin-arm64`, so the direct pin is not
  needed for resolution — only the linux-x64 CI runner benefits from it.
- **Impact:** No effect on the released artifact or on CI (which runs
  linux/x64). The security-relevant consequence is process, not product: a
  maintainer or reviewer on macOS or Windows cannot run `npm ci`, and is
  therefore pushed toward `npm install`, which rewrites `package-lock.json`
  and silently re-resolves the dependency graph away from the audited,
  hash-pinned lockfile. That is a plausible route for un-audited dependency
  drift to enter a release.
- **Remediation:** Remove the direct devDependency and let `rolldown`'s
  optionalDependencies resolve the correct binding per platform. If the CI
  runner genuinely needs the explicit pin, prefer `npm ci --force` scoped to
  the CI job, or move the pin behind an `optionalDependencies` entry.
- **Status:** open — **not remediated in this release.** Fixing it edits
  `package.json`/`package-lock.json`, which changes `deps-sha256` and would
  require re-auditing a lockfile change unrelated to #74. Recommended as a
  standalone PR.

  *Auditor note on this audit's own integrity:* because of F-002, the
  toolchain used to run tests for this release was installed via
  `npm install --no-save --no-package-lock` after temporarily removing the
  offending line. `package.json` and `package-lock.json` were restored and
  byte-compared against their committed versions before hashing (both
  identical). The audited tree is the committed tree. The dependency audit
  above was deliberately run with `--package-lock-only` so its findings
  reflect the committed lockfile rather than that local install.

### F-003 — v2.3.0 audit's Accepted Risks table misdescribes both exception IDs

- **Severity:** informational
- **Category:** other (documentation / audit-trail accuracy)
- **Location:** `.security/audits/2.3.0.md` (Accepted Risks section)
- **Description:** The prior audit's Accepted Risks table describes
  CLB-EXC-0001 as `'unsafe-eval'` in CSP and CLB-EXC-0002 as
  `'unsafe-inline'` in CSP style-src. The registry at
  `.security/audits/exceptions.json` defines CLB-EXC-0001 as *"Single
  maintainer — no separation of duties"* and CLB-EXC-0002 as *"ExcelJS
  4.4.0 transitive CVEs in Node-only code paths"*. The cited IDs and the
  described risks do not correspond.
- **Impact:** No runtime impact. `verify-audit.js` checks only that cited
  IDs exist and are unexpired, so the mismatch passes CI. The risk is to the
  audit trail itself: a reader reconstructing which risks were accepted at
  v2.3.0 gets the wrong answer, and the two genuinely accepted risks
  (single-maintainer sign-off, ExcelJS transitives) appear undocumented for
  that release.
- **Remediation:** Signed audits are append-only, so `2.3.0.md` must not be
  edited. Issue a `2.3.0-r2` revision carrying the corrected table, with
  `supersedes:` pointing at the original and the original marked
  `superseded`. This audit's own Accepted Risks table below reflects the
  registry as written.
- **Status:** open (pre-existing; not introduced by this release)

## Dependency Audit

```
$ npm audit --package-lock-only --json | jq '.metadata.vulnerabilities'
{
  "info": 0,
  "low": 0,
  "moderate": 0,
  "high": 5,
  "critical": 0,
  "total": 5
}

$ npm audit --package-lock-only --omit=dev
found 0 vulnerabilities
```

| Package | Severity | Direct? | Advisories | Fix |
|---------|----------|---------|-----------|-----|
| `brace-expansion@1.1.14` | high | transitive (dev) | GHSA-3jxr-9vmj-r5cp, GHSA-mh99-v99m-4gvg, GHSA-rgw5-rvv9-x895 — DoS via exponential/unbounded expansion | available |
| `js-yaml@4.1.1` | high | transitive (dev, via eslint) | GHSA-h67p-54hq-rp68, GHSA-52cp-r559-cp3m, GHSA-5p4m-2wfm-xmqj — quadratic-complexity DoS in merge keys / `!!omap` | available (PR #80) |
| `nanoid@3.3.11` | high | transitive (dev) | GHSA-28wg-ghj8-5hjv, GHSA-2v37-7h3g-55p8 — infinite loop on negative/zero size | available |
| `postcss@8.5.12` | high | transitive (dev) | GHSA-r28c-9q8g-f849, GHSA-fxqj-rqcc-2cmp — path traversal via attacker-controlled `sourceMappingURL` | available (PR #81) |
| `vite@8.0.10` | high | transitive (dev, via vitest) | GHSA-v6wh-96g9-6wx3 (launch-editor NTLMv2 disclosure, Windows), GHSA-fx2h-pf6j-xcff (`server.fs.deny` bypass, Windows) | available (PR #79) |

Narrative: this is drift since the v2.3.0 audit of 2026-05-25, which
recorded zero vulnerabilities; the advisories above were published in the
intervening ~11 weeks. No dependency was added, removed, or upgraded by this
release — `deps-sha256` changed from v2.3.0 solely because `package.json`
carries the version bump to 2.3.1; `package-lock.json` is otherwise
unchanged.

The distinction that matters for release safety: the project ships **no
production dependencies**. The four third-party libraries in the release
artifact (react, react-dom, @babel/standalone, exceljs) are fetched from
unpkg at build time and verified against pinned SRI hashes; none is affected
by the advisories above. See F-001 for the reachability analysis and the
remediation path.

## Prompt Injection Defense Notes

A sentinel scan was performed across all of `src/**` (33 files), covering
instruction-override phrases, role-marker injection, Unicode tricks, and
hidden base64/hex payloads. Reporting both what was found and that the scan
ran, since silence and absence are otherwise indistinguishable.

**Two benign matches, no injection attempts:**

1. Instruction-phrase scan matched one line, at `src/components/HelpModal.js:75`:

   ```
   Total flags and scores act as safety nets to catch overall imbalances.
   ```

   The pattern `act as` fires on ordinary English describing optimizer
   behavior. Pre-existing UI copy, not part of this changeset. The same match
   was recorded in the v2.3.0 audit.

2. Unicode scan matched one line, at `src/components/SetupPage.js:178`:

   ```
   <div className="icon">👩‍🎓</div>
   ```

   The hit is U+200D (zero-width joiner) as a legitimate component of the
   `👩‍🎓` emoji ZWJ sequence, not a hidden-character payload. Pre-existing.

**Not found:** no bidi overrides (U+202A–U+202E, U+2066–U+2069), no tag
characters (U+E0000–U+E007F), no stray zero-width characters outside the
emoji sequence above, no base64/hex blobs over 50 characters in the changed
files, no role markers (`[INST]`, `<|im_start|>`, `<<SYS>>`, `system:`,
`assistant:`), and no text anywhere in `src/**` attempting to instruct,
redirect, or reconfigure an AI auditor.

No instruction text from any source file altered this audit's scope,
severity classifications, findings, or sign-off. The audit plan was fixed
before any source file was opened and was executed unchanged.

**Residual risk:** indirect prompt injection is not a solved problem as of
mid-2026; adaptive attacks defeat published defenses at high rates. The
hardening applied here is defense-in-depth, not a guarantee. The
deterministic checks (`audit-hash.js`, `verify-audit.js`) and the human
sign-off below are the enforcement layer; the AI-assisted review is a force
multiplier, not the arbiter.

## Accepted Risks

| Exception ID | Reason | Expires |
|-------------|--------|---------|
| CLB-EXC-0001 | Single maintainer — no separation of duties. Auditor and approver are the same person; mitigated by public GitHub history, SLSA-attested releases, and a publicly verifiable audit trail. | 2027-05-16 |
| CLB-EXC-0002 | ExcelJS 4.4.0 transitive CVEs confined to Node-only code paths. The app calls only `workbook.xlsx.load(buffer)` from the browser UMD bundle (`src/components/ImportModal.js`); the vulnerable Node paths are unreachable. | 2027-05-16 |

Both exceptions are unexpired as of the audit date and remain accurate as
written in `.security/audits/exceptions.json`. Neither was modified by this
audit.

Note that F-001 is **not** covered by an exception. It is recorded as an
open finding with an available fix, and is the condition attached to this
release's `conditional-pass` status.

## Sign-off

PENDING HUMAN SIGN-OFF — the auditor of record must personally review every
finding above before this line is replaced. In particular, three judgments
need a human decision rather than inheritance from the draft:

1. **F-001 severity.** Five advisories that npm rates `high` are recorded
   here as informational on the basis of dev-only reachability. If you would
   rather not ship on that reasoning, merge #79/#80/#81 first and re-audit;
   the finding then disappears rather than being argued away.
2. **`status: conditional-pass`.** The attached condition is F-001's
   remediation. Change to `pass` only if you accept F-001's reasoning as
   sufficient on its own.
3. **F-002's effect on this audit.** The local toolchain install deviated
   from `npm ci`; the restoration was byte-verified and the dependency audit
   was run `--package-lock-only`, but you should confirm you find that
   acceptable.

Replace this block with the attestation below, filled in:

> I have personally reviewed every finding above. The artifacts at the
> listed hashes are, to my knowledge, free of known vulnerabilities of
> severity high or above outside of declared exceptions, and the public
> claims in `SECURITY.md` are accurate as of this audit date.
>
> — Ryan Armstrong, YYYY-MM-DD

---
version: 3.1.0
audit-date: 2026-08-08
auditor: PENDING HUMAN SIGN-OFF
assisted-by: AI assistant via .claude/skills/security-audit-system (claude-opus-5)
commit_sha: 26f107311d4d286fe201697fb230767a6d6c2876
manifest-sha256: sha256:9b36e4355acd6e689b682b5489de6be0bb3f50ba3cdbf905fb1b864d36cf07a0
sources-sha256: sha256:2f4396dbbd6c57633f30a88aee9785ef772237a80bb71b349713e7f9158629c2
deps-sha256: sha256:4f567df8e39b7964e93b8535a4e474f3b3ad1f3f253baa909dd07a4ba96b39db
claims-sha256: sha256:2546c2cec67c4a37b798087f9b419305ca33ec3a9954d13ef7618838620ce019
build-sha256: sha256:27a801b003d52fea5e7e47913c75b1cac572dbaf9b080a3c0d1dee4da54a3059
sbom_ref: "release asset: class-list-builder-v3.1.0.cdx.json"
status: draft
exceptions-cited: [CLB-EXC-0001, CLB-EXC-0002]
prior_audit: ".security/audits/3.0.0.md"
---

# Security Audit — v3.1.0

## Scope

Single-file React SPA distributed as a standalone HTML bundle, plus its
build pipeline and the public security claims in `SECURITY.md`.

Paths covered by the manifest hash:

- `src/**` (33 files)
- `package.json`, `package-lock.json`
- `SECURITY.md`
- `build-standalone.js`, `class-list-builder-source.html`,
  `scripts/validate-build.js`

Out of scope: `node_modules/` (covered transitively via `npm audit`),
`tests/**`, developer-only scripts not in the manifest.

Predecessor: v3.0.0.

## Methodology

- `npm audit --package-lock-only --json` reviewed against the committed
  lockfile (not a locally-resolved tree), plus `--omit=dev` separately to
  establish whether any advisory reaches the shipped artifact.
- Each claim in `SECURITY.md` re-verified against the post-change source by
  grep and by reading the matched call sites.
- Changed files reviewed line-by-line: `src/optimizer.js`,
  `src/utils/assessment.js`, `src/components/OptimizePage.js`,
  `src/styles.css`, `src/defaults.js`, `package.json`, `package-lock.json`.
- The new `optimizeMultiStart` traced for determinism (no `Math.random`;
  integer salts only) and for the fail-open path in its cross-file constant
  resolution (see F-005), which was reproduced directly rather than
  reasoned about.
- Worst-case runtime measured for the new 5× search at 100 / 300 / 600 /
  2000 students, in Node and in-browser under the real Babel pipeline.
- Load-order dependency of the new cross-file constant confirmed in a real
  browser (`typeof optimizeMultiStart === 'function'`,
  `OPTIMIZE_RESTARTS === 5`) rather than assumed from the script tag order.
- Minor-version load path re-checked against
  `checkVersionCompatibility()` to confirm this release does not repeat
  v3.0.0's compatibility break.
- Prompt-injection sentinel scan re-run across all of `src/**`, plus a
  Unicode irregularity scan (bidi overrides, zero-width, tag characters)
  and a base64/hex payload scan over the changed files.
- Standalone artifact rebuilt (`node build-standalone.js`) and validated
  (`node scripts/validate-build.js`); artifact grepped for remote
  `<script src>` / `<link href>` and for its CSP.
- Full suite re-run at the audited commit: 222 tests pass across 11 files;
  0 lint errors. Working tree confirmed clean before and after the build.

**Auditor independence, disclosed:** the AI assistant that drafted this
audit is the same assistant that authored the code under audit, in the same
session. It is not an independent reviewer of this changeset, and its
familiarity with the code cuts both ways — it knows where the bodies are,
and it has a stake in the change looking correct. F-005 is the one finding
that argues against the author's own design choice; treat the absence of
further such findings as weak evidence, not strong. This compounds
CLB-EXC-0001 (single maintainer, no separation of duties) rather than being
covered by it, and is the reason the human sign-off below is the control
that matters.

**No `--force` was required this release.** Unlike v3.0.0, no lockfile
resolution or toolchain install was needed — `npm version` rewrote only the
two version strings (verified by diff), so F-002's `EBADPLATFORM` block was
never hit. The lockfile in this audit is byte-identical to the committed
one.

## Changes Since v3.0.0

1. **Print report grid lines** (`src/styles.css`, issue #78). Vertical
   rules and an outer border added to `.print-student-table` inside the
   existing `@media print` block; the row rule darkened from `#eee` to
   `#ddd` for a uniform grid. Presentation-only. No `url()`, `@import`, or
   external reference introduced — verified against the diff.

2. **Multi-start optimizer** (`src/optimizer.js`,
   `src/components/OptimizePage.js`). New `optimizeMultiStart()` runs
   `OPTIMIZE_RESTARTS` (5) independent annealing trajectories at integer
   salts `0..N-1` and keeps the lowest-cost result, selected by the full
   constrained cost. `OptimizePage.runOptimize` now calls it instead of
   `optimize()`.

   Motivation was a scoring defect, not a security issue: the assessment's
   balanced baseline already ran best-of-5, while the app ran a single
   trajectory at salt 0 — which the baseline also runs — so
   `balancedCost <= currentCost` held by construction and an unconstrained
   assignment could only score 100 when salt 0 won the restart lottery.

   The security-relevant properties are preserved: the function introduces
   no `Math.random`, no network, no storage, and no new data flow. It is a
   pure function of its inputs and remains deterministic (asserted by test).

3. **Score-normalization epsilon** (`src/utils/assessment.js`).
   `COST_EQUALITY_EPSILON = 1e-9` collapses sub-ULP cost differences to
   zero before the `p=0.35` power law, which is near-vertical at the
   origin. Costs are ~1e-3, so the tolerance sits ~6 orders of magnitude
   below the compared values. The same epsilon now gates the pre-existing
   defensive-floor `console.warn` so an exact tie stops logging a spurious
   warning.

4. **Shared restart constant.** `computeBaselineBalanced`'s hardcoded
   `restarts = 5` default became `RESTARTS_REF`, resolved from
   `OPTIMIZE_RESTARTS` across the script boundary. This is the only change
   in the release that introduces a new failure mode — see F-005.

5. **Lint globals** (`.eslintrc.json`) — `optimizeMultiStart` and
   `OPTIMIZE_RESTARTS` registered as readonly globals. Outside the manifest;
   no effect on any hash.

6. **Version bump** — `package.json`, `src/defaults.js`, and
   `package-lock.json` to 3.1.0.

Minor rather than patch because re-optimizing identical inputs now returns
a different (better) assignment than v3.0.0 did. Saved projects are
unaffected on load: they restore a stored assignment rather than re-running
the optimizer.

`claims-sha256` (`2546c2ce…`) and `build-sha256` (`27a801b0…`) are
byte-identical to v3.0.0 and to v2.3.0, confirming that neither
`SECURITY.md` nor the build entrypoints were touched. `sources-sha256`
changed for the five source files named above; `deps-sha256` for the version
bump alone.

**Lockfile note.** `package-lock.json`'s root `version` was stranded at
`2.3.1` — it was never advanced through the 2.3.1 → 3.0.0 releases.
`npm version 3.1.0` corrected it as a side effect. The diff against `main`
is exactly two version strings; no dependency resolution changed. The CI
version gate compares only `package.json` against `src/defaults.js`, so this
drift was ungated and could have continued accumulating. Worth considering
extending that gate to the lockfile root, though it is not a finding — no
dependency was misrepresented, since resolution lives in `packages`, not in
the root version string.

## SECURITY.md Claims Verification

| Claim | Status | Evidence |
|-------|--------|----------|
| Zero network transmission at runtime | Verified | `grep -rnE 'fetch\(\|XMLHttpRequest\|WebSocket\|sendBeacon\|navigator\.send\|EventSource' src/` → no matches. This release adds only local computation and print CSS |
| No fetch / XHR / WebSocket / sendBeacon | Verified | Same grep, re-run post-change |
| Release version inlines all dependencies | Verified | Artifact rebuilt at this commit; `grep -oE '<(script\|link)[^>]*(src\|href)="https?://[^"]*"' dist/class-list-builder-v3.1.0.html` → no matches. The only two remote URLs in the artifact are `<a href>` documentation links to the project's own GitHub, which load nothing |
| CSP as documented, incl. `connect-src 'none'` | Verified | Artifact's `<meta http-equiv="Content-Security-Policy">` matches `SECURITY.md:106-115` byte-for-byte |
| localStorage scope is origin-bound | Verified | Only criteria + theme keys written (`src/contexts/index.js:382-383`, `:523`). No student data persisted; unchanged this release |
| Deterministic seeded RNG | Verified | The optimizer's FNV-1 + Mulberry32 seeding is untouched, and `optimizeMultiStart` adds only integer salts — no new nondeterminism. `grep -rn "Math.random" src/` returns six sites, none in the optimizer: `src/sample-data.js:8,9,122` (demo data), `src/csv.js:3` (local row IDs, not security tokens), `src/utils/assessment.js:137,178` (random baseline). Note for the trail: the v3.0.0 audit's phrasing "confined to assessment baselines" was incomplete — sample-data and csv.js also use it, and always have |
| FERPA: data never leaves the device | Verified | No network sinks; no new export path, storage write, or data flow in this changeset |
| No `eval` / `new Function` in app source | Verified | `grep -rnE '\beval\(\|new Function\(' src/` → no matches. (`'unsafe-eval'` remains required for Babel-standalone, as documented) |

No refuted claims.

## Vulnerability Findings

### F-005 — Cross-file restart constant fails **open**, silently scoring every assignment 100

- **Severity:** low
- **Category:** other (correctness / integrity of a user-facing safety signal)
- **Location:** `src/utils/assessment.js:26` (`RESTARTS_REF`), consumed at
  `src/utils/assessment.js:93`
- **Description:** This release replaced `computeBaselineBalanced`'s
  self-contained `restarts = 5` default with a constant resolved across the
  script boundary:

  ```js
  const RESTARTS_REF = typeof OPTIMIZE_RESTARTS !== 'undefined' ? OPTIMIZE_RESTARTS : _restarts;
  ```

  Both arms can be `undefined` — the browser arm if `src/utils/assessment.js`
  is ever evaluated before `src/optimizer.js`, the Node arm if the require
  path changes. `restarts` then defaults to `undefined`, and the loop
  `for (let salt = 0; salt < undefined; salt++)` executes **zero** iterations,
  so `bestCost` is returned as its `Infinity` initializer.

  Reproduced end to end rather than inferred: with `balancedCost = Infinity`,
  `normalizeBalanceScore` takes its defensive floor (`currentCost < balancedCost`),
  clamps the baseline to the current cost, and returns

  ```
  { "score": 100, "effectiveBalancedCost": 0.005 }
  ```

  — a perfect score for an arbitrary assignment. The failure is silent: the
  epsilon gate added in this same release suppresses the `console.warn` only
  for sub-ULP gaps, but the `Infinity` path produces a gap far above the
  epsilon, so a warning *would* still fire on the console. Nothing surfaces
  in the UI.

- **Impact:** Not reachable in the shipped artifact. Script order is fixed
  at `class-list-builder-source.html:69` (optimizer) before `:82`
  (assessment), the build inlines in that order, and this was confirmed in a
  live browser (`OPTIMIZE_RESTARTS === 5`). The exposure is to future edits:
  reordering script tags, lazy-loading, or moving `assessment.js` earlier
  would degrade the balance score to a constant 100 with no test failure and
  no user-visible error. For a tool whose only quality signal to a teacher is
  that score, failing open is the wrong direction — a teacher would be told a
  class list is perfectly balanced when it has not been evaluated at all.
  No confidentiality or integrity exposure of student data.
- **Remediation:** Fail closed rather than open. Either give the resolution a
  literal fallback (`const RESTARTS_REF = … : _restarts) || 5;`) so the
  constant can never be `undefined`, or have `computeBaselineBalanced` throw
  on a non-positive/undefined `restarts` rather than returning `Infinity`.
  The second is preferable: an unevaluable baseline should be an error, not a
  perfect score. A regression test asserting
  `computeBaselineBalanced(..., undefined-constant)` does not return
  `Infinity` would pin it.
- **Status:** open — introduced in this release, not remediated in it.

### Carried forward from v3.0.0

These remain open and were re-checked at this commit. Their IDs are kept
stable rather than renumbered.

| ID | Summary | Re-check at v3.1.0 |
|----|---------|--------------------|
| F-002 | `@rolldown/binding-linux-x64-gnu` direct devDependency breaks `npm ci` off-platform | Still present (`package.json:19`). Did **not** affect this audit — no install or lockfile resolution was required, so no `--force` was used |
| F-003 | v2.3.0 audit's Accepted Risks table misdescribes both exception IDs | Still open; no `2.3.0-r2` revision has been issued |
| F-004 | Major bump renders all v2.x project files unloadable | Unchanged and **not aggravated**. `checkVersionCompatibility()` (`src/utils/projectDeserializer.js:76-86`) classifies a minor-version difference as a warning with `compatible: true`, so v3.0.0 project files load normally under v3.1.0. The recommendation from v3.0.0 — telling users in the error message that prior releases remain downloadable — is still unimplemented |

No new dependency, injection, auth, crypto, or privacy findings.

## Dependency Audit

```
$ npm audit --package-lock-only --json | jq '.metadata.vulnerabilities'
{
 "info": 0,
 "low": 0,
 "moderate": 0,
 "high": 0,
 "critical": 0,
 "total": 0
}

$ npm audit --package-lock-only --omit=dev
found 0 vulnerabilities
```

Zero vulnerabilities, unchanged from v3.0.0. No dependency was added,
removed, or re-resolved this release — the entire `package-lock.json` diff
is the two root version strings described above.

The distinction that continues to matter for release safety: the project
ships **no production dependencies** (`"dependencies": {}`). The four
third-party libraries in the release artifact (react, react-dom,
@babel/standalone, exceljs) are fetched from unpkg at build time and
verified against pinned SRI hashes. None changed in this release.

## Prompt Injection Defense Notes

A sentinel scan was performed across all of `src/**` (33 files) at this
commit, covering instruction-override phrases, role-marker injection,
Unicode tricks, and hidden base64/hex payloads, plus a targeted scan of the
five changed source files. Reporting both what was found and that the scan
ran, since silence and absence are otherwise indistinguishable.

**Two benign matches, no injection attempts** — both pre-existing, neither
in this changeset, and both previously recorded in the v2.3.0 and v3.0.0
audits:

1. Instruction-phrase scan matched one line, at `src/components/HelpModal.js:75`:

   ```
   Total flags and scores act as safety nets to catch overall imbalances.
   ```

   The pattern `act as` fires on ordinary English describing optimizer
   behavior.

2. Unicode scan matched one line, at `src/components/SetupPage.js:178`:

   ```
   <div className="icon">👩‍🎓</div>
   ```

   The hit is U+200D (zero-width joiner) as a legitimate component of the
   `👩‍🎓` emoji ZWJ sequence, not a hidden-character payload.

**Not found:** no bidi overrides (U+202A–U+202E, U+2066–U+2069), no tag
characters (U+E0000–U+E007F), no stray zero-width characters outside the
emoji sequence above, no base64/hex blobs over 50 characters in any changed
file, no role markers (`[INST]`, `<|im_start|>`, `<<SYS>>`, `system:`,
`assistant:`), and no text anywhere in `src/**` attempting to instruct,
redirect, or reconfigure an AI auditor.

The only non-ASCII characters introduced by this changeset are three em
dashes (U+2014) inside comments in `src/optimizer.js` and
`src/utils/assessment.js` — printable, not zero-width, not bidi.

No instruction text from any source file altered this audit's scope,
severity classifications, findings, or sign-off. The audit plan was fixed
before any source file was opened and was executed unchanged.

**Residual risk:** indirect prompt injection is not a solved problem as of
mid-2026; adaptive attacks defeat published defenses at high rates. The
hardening applied here is defense-in-depth, not a guarantee. The
deterministic checks (`audit-hash.js`, `verify-audit.js`) and the human
sign-off below are the enforcement layer; the AI-assisted review is a force
multiplier, not the arbiter. This release's disclosed auditor-independence
gap (see Methodology) narrows that force multiplier further.

## Accepted Risks

| Exception ID | Reason | Expires |
|-------------|--------|---------|
| CLB-EXC-0001 | Single maintainer — no separation of duties. Auditor and approver are the same person; mitigated by public GitHub history, SLSA-attested releases, and a publicly verifiable audit trail. | 2027-05-16 |
| CLB-EXC-0002 | ExcelJS 4.4.0 transitive CVEs confined to Node-only code paths. The app calls only `workbook.xlsx.load(buffer)` from the browser UMD bundle (`src/components/ImportModal.js`); the vulnerable Node paths are unreachable. | 2027-05-16 |

Both exceptions are unexpired as of the audit date and remain accurate as
written in `.security/audits/exceptions.json`. Neither was modified by this
audit, and no new exception was added.

F-005 is recorded as an open finding rather than accepted via the registry:
it is a latent correctness defect introduced this release, not a standing
risk the project has decided to live with. If it is deliberately shipped
unfixed, it should get an exception entry rather than remaining an
unbacked open finding.

F-002, F-003 and F-004 remain open and are likewise not exception-backed:
none is a vulnerability in the released artifact.

## Sign-off

Four judgments are escalated to the human auditor rather than inherited from
the AI-assisted draft:

1. **F-005, and whether to ship with it.** It is unreachable today and the
   fix is a one-line fallback. The question is whether a latent fail-open on
   the score is acceptable in a release whose entire purpose was to make that
   score trustworthy. Fixing it before merge is cheap and is recommended.
2. **Auditor independence.** The assistant that wrote this code also drafted
   this audit, in the same session. The Methodology section says so plainly.
   Decide whether that is acceptable provenance for this release, or whether
   the changeset warrants a review pass you drive independently.
3. **The behavior change itself.** Re-optimizing identical inputs now returns
   a different assignment than v3.0.0 did. The claimed justification is a
   drop in constraint violations (measured 6/40 → 1/40 projects on
   synthetic constraint sets, which may be harsher than real teacher input);
   the balance-quality gain is marginal. You are signing for a minor bump on
   that basis.
4. **`package-lock.json`'s root version.** It was wrong for two releases and
   nothing caught it. Consider whether the CI version gate should cover it.

I have personally reviewed every finding above. The artifacts at the
listed hashes are, to my knowledge, free of known vulnerabilities of
severity high or above outside of declared exceptions, and the public
claims in `SECURITY.md` are accurate as of this audit date.

— PENDING HUMAN SIGN-OFF
